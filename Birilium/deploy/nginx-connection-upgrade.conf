# /etc/nginx/conf.d/connection-upgrade.conf
#
# Map http Upgrade header to a Connection value so reverse-proxied WebSocket
# upgrades work. Required by node.birilium.com vhost.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
